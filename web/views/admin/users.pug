extends ../base.pug

block content

    h1 Users

    ul.list-group
        each val, i in users
            li.list-group-item #{val.username}
    
    nav.d-flex.flex-column.justify-content-center.align-items-center
        ul.pagination
            li.page-item
                a.page-link(href=prev_href) Prev
            li.page-item
                a.page-link(href="#") 1
            li.page-item
                a.page-link(href="#") 2
            li.page-item
                a.page-link(href="#") 3
            li.page-item
                a.page-link(href=next_href) Next
